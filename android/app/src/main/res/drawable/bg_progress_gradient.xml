<?xml version="1.0" encoding="utf-8"?>
<!-- Progress bar gradient matching website aesthetic -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <solid android:color="@color/input_background" />
            <corners android:radius="10dp" />
            <stroke android:width="1dp" android:color="@color/border_color" />
        </shape>
    </item>
    <!-- Progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <gradient
                    android:startColor="@color/iisu_gradient_start"
                    android:centerColor="@color/iisu_gradient_mid"
                    android:endColor="@color/iisu_gradient_end"
                    android:angle="0" />
                <corners android:radius="9dp" />
            </shape>
        </clip>
    </item>
</layer-list>
